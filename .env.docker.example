# =============================================================================
# POS — Docker Compose environment (copy to .env and set values)
# =============================================================================

# -----------------------------------------------------------------------------
# PostgreSQL (used by docker-compose for postgres service and DATABASE_URL)
# -----------------------------------------------------------------------------
POSTGRES_USER=posuser
POSTGRES_PASSWORD=change-me-secure-password
POSTGRES_DB=pos

# -----------------------------------------------------------------------------
# Required: POS JWT signing secret (min 16 characters)
# -----------------------------------------------------------------------------
POS_JWT_SECRET=your-production-jwt-secret-min-16-chars

# -----------------------------------------------------------------------------
# Optional: App URL (for Pesapal callbacks and browser origin)
# -----------------------------------------------------------------------------
# APP_BASE_URL=https://pos.example.com
# NEXT_PUBLIC_APP_ORIGIN=https://pos.example.com

# -----------------------------------------------------------------------------
# Optional: Pesapal, backups, cafe name
# -----------------------------------------------------------------------------
# PESAPAL_BASE_URL=https://cybqa.pesapal.com/pesapalv3/api
# PESAPAL_CONSUMER_KEY=
# PESAPAL_CONSUMER_SECRET=
# PESAPAL_IPN_ID=
# PESAPAL_CALLBACK_URL=
# PESAPAL_WEBHOOK_SECRET=
# BACKUP_DIR=/app/backups
# CAFE_NAME=Café Havilah
