# POS — Nginx reverse proxy
# Place TLS certs in deploy/ssl/ (fullchain.pem, privkey.pem) and uncomment the HTTPS server block below.

events {
    worker_connections 1024;
}

http {
    upstream pos_app {
        server pos-app:3000;
    }

    server {
        listen 80;
        server_name _;
        client_max_body_size 10M;

        location / {
            proxy_pass http://pos_app;
            proxy_http_version 1.1;
            proxy_set_header Host $host;
            proxy_set_header X-Real-IP $remote_addr;
            proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
            proxy_set_header X-Forwarded-Proto $scheme;
            proxy_buffering off;
            proxy_read_timeout 3600s;
            proxy_send_timeout 60s;
        }

        # Optional: HTTPS redirect when using TLS
        # return 301 https://$host$request_uri;
    }

    # Uncomment and adjust when you have TLS certs in deploy/ssl/
    # server {
    #     listen 443 ssl;
    #     server_name your-domain.com;
    #     ssl_certificate     /etc/nginx/ssl/fullchain.pem;
    #     ssl_certificate_key /etc/nginx/ssl/privkey.pem;
    #     client_max_body_size 10M;
    #     location / {
    #         proxy_pass http://pos_app;
    #         proxy_http_version 1.1;
    #         proxy_set_header Host $host;
    #         proxy_set_header X-Real-IP $remote_addr;
    #         proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    #         proxy_set_header X-Forwarded-Proto $scheme;
    #         proxy_buffering off;
    #         proxy_read_timeout 3600s;
    #         proxy_send_timeout 60s;
    #     }
    # }
}
