# =============================================================================
# POS — Environment variables (copy to .env and set values)
# .env is gitignored; never commit credentials.
# =============================================================================

# -----------------------------------------------------------------------------
# Required: PostgreSQL
# -----------------------------------------------------------------------------
DATABASE_URL="postgresql://USER:PASSWORD@HOST:PORT/DATABASE?schema=public"

# Example (local):
# DATABASE_URL="postgresql://postgres:postgres@localhost:5432/pos?schema=public"

# -----------------------------------------------------------------------------
# Required: POS authentication (JWT signing). Min 16 characters.
# -----------------------------------------------------------------------------
POS_JWT_SECRET="your-secret-at-least-16-chars"

# -----------------------------------------------------------------------------
# Optional: Redis (realtime multi-instance). Required when running multiple app instances behind a load balancer.
# -----------------------------------------------------------------------------
# REDIS_URL="redis://localhost:6379"

# -----------------------------------------------------------------------------
# Optional: Pesapal (card / MoMo / Airtel payments)
# -----------------------------------------------------------------------------
# PESAPAL_BASE_URL="https://cybqa.pesapal.com/pesapalv3/api"
# PESAPAL_CONSUMER_KEY=""
# PESAPAL_CONSUMER_SECRET=""
# PESAPAL_IPN_ID=""
# PESAPAL_CALLBACK_URL=""
# PESAPAL_WEBHOOK_SECRET=""  # Secret for HMAC verification of webhook calls

# -----------------------------------------------------------------------------
# Optional: App base URL (for Pesapal callback and redirect)
# -----------------------------------------------------------------------------
# APP_BASE_URL="http://localhost:3000"
# NEXT_PUBLIC_APP_ORIGIN="http://localhost:3000"
